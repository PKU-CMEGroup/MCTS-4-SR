extends: ../../basic.yaml

output:
  results_dir: benchmark_results/ablations/model_b

search:
  # Model B: disable GP-based jump actions
  # p_s = 0: disable GP-based jump actions
  gp_rate: 0.0
  # Unused when gp_rate = 0, kept explicit for clarity
  mutation_rate: 0.0
